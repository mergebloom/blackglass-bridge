# Validation records

This directory contains sanitized, non-proprietary release summaries. Raw
profiles, vaults, screenshots, credentials, databases, patched ASARs, and app
bundles stay under ignored `.data/` storage and must never be committed.

A current release record is valid only when it names the exact renderer and
server artifact hashes. Historical records demonstrate protocol behavior but
do not qualify a rebuilt or renamed artifact.
